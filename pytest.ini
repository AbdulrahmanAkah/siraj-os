[pytest]
testpaths =
    tests/unit
    tests/integration
    tests/regression
    tests/smoke
python_files = test_*.py
pythonpath = .
norecursedirs =
    archive
    tests/legacy
markers =
    external_ai: explicit opt-in for a live external AI provider; excluded from default verification
    integration: local integration test that may require an installed executable
